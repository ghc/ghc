module MatchPatComments where

expandProcess
        outCHAs -- c0
        locationDescr =
    blah

next
    ( steps  -- c1
    , ys     -- c2
    ) x      -- c3
    = (steps, x, ys)

makeProjection
    Function{funMutual = VV, -- c4
             funAbstr = ConcreteDef} = undefined
