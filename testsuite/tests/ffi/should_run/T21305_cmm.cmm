#include "Cmm.h"

f(P_ a, P_ b, P_ c) {
  I64[c + SIZEOF_StgArrBytes + 8] = 770000;
  // c is a MutableByteArray#; unlifted boxed results carry tag 1.
  // See Note [Pointer tagging of unlifted boxed primitives] in GHC.StgToCmm.Prim.
  return (b, a, c + 1);
}
