#include "Cmm.h"

aToWordzh (P_ clos)
{
    return (clos);
}

reallyUnsafePtrEqualityUpToTag (W_ clos1, W_  clos2)
{
    clos1 = UNTAG(clos1);
    clos2 = UNTAG(clos2);
    return (clos1 == clos2);
}
