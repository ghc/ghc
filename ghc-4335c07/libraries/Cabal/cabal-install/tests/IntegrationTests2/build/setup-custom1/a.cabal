name: a
version: 0.1
build-type: Custom
cabal-version: >= 1.10

-- explicit setup deps:
custom-setup
  setup-depends: base, Cabal >= 1.18

library
  exposed-modules: A
  build-depends: base
  default-language: Haskell2010
